<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <div class="greeting">
        こんにちは、
        <%# <%= link_to "#{current_user.name}さん", prototype_user_path(prototype.prototype_id, user.id), class: :greeting__link %>
        <%= link_to "#{current_user.name}さん", "/users/#{current_user.id}", class: :greeting__link %>
      </div>
    <% end %>
    <div class="card__wrapper">
      <%= render partial: "prototype", collection: @prototyoes %>
    </div>
  </div>
</main>
